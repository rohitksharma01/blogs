source "https://rubygems.org"

ruby ">= 3.1", "< 3.6"

# Core site engine
gem "jekyll", "~> 4.3"

# Needed for serving in Ruby 3+
gem "webrick", "~> 1.8"

# Use the Midnight theme via local gem to avoid remote SSL fetches
gem "jekyll-theme-midnight", "~> 0.2"

# Optional: If you later want to use remote themes, re-enable:
# group :jekyll_plugins do
#   gem "jekyll-remote-theme", "~> 0.4"
# end
